import ambientLightOptions from "@app/environment/lights/ambient/options";
import sunLightOptions from "@app/environment/lights/sunlight/options";

const config = {
  ambientLightOptions,
  sunLightOptions,
  envMapOptions: {
    intensity: 0.4,
  },
};

export default config;
